# Poketmon Console Battle

Windows Console API와 C++로 구현한 포켓몬 스타일의 턴제 배틀 게임입니다.  
콘솔의 커서 위치와 글자 색상을 직접 제어해 전투 화면을 구성하고, 속성 상성·상태 이상·능력치 변화·아이템·교체·성장 등 RPG 전투 규칙을 구현했습니다.

> 포켓몬 5마리로 파티를 구성해 서로 다른 특성을 지닌 사천왕 4명을 차례로 공략하는 싱글 플레이 프로젝트입니다.

## 프로젝트 개요

| 항목 | 내용 |
| --- | --- |
| 개발 언어 | C++ |
| 개발 환경 | Visual Studio 2022, Windows SDK |
| UI | Windows Console API |
| 장르 | 턴제 RPG 배틀 |
| 콘텐츠 | 포켓몬 20종, 기술 57종, 타입 11종, 아이템 5종, 사천왕 4스테이지 |

## 주요 기능

- **턴제 전투**: 기술 사용, 아이템 사용, 포켓몬 교체, 도망치기 선택지를 제공하며 스피드 능력치에 따라 행동 순서를 결정합니다.
- **기술 시스템**: 각 포켓몬은 4개의 기술을 사용합니다. 기술별로 위력, 명중률, 물리/특수 분류, 속성, 상태 이상 확률, 능력치 변화, 회복 효과를 데이터로 관리합니다.
- **데미지 계산**: 공격·방어 또는 특수공격·특수방어 능력치를 구분하고, 자속 보정, 속성 상성, 급소 배율을 조합해 최종 데미지를 계산합니다.
- **상태 이상**: 독, 마비, 화상, 얼음, 수면, 혼란을 구현했습니다. 턴 경과 피해와 행동 불가 확률, 해제 확률 등 상태별 규칙이 전투에 반영됩니다.
- **능력치 랭크**: 공격, 방어, 특수공격, 특수방어, 스피드, 회피 랭크의 상승·하락을 처리하고 전투 능력치에 적용합니다.
- **파티 및 아이템**: 전투 중 포켓몬을 교체하거나 회복약, 부활, 상태 이상 치료제, 스피드 강화 아이템을 사용할 수 있습니다.
- **성장과 진행**: 전투 참여 포켓몬에게 경험치를 분배하고, 레벨 업과 피카츄의 진화 이벤트를 처리합니다.
- **콘솔 UI**: `SetConsoleCursorPosition`과 `SetConsoleTextAttribute`를 이용해 HP/EXP 바, 상태 정보, 메뉴, 포켓몬 ASCII 아트와 스테이지별 타이틀 연출을 출력합니다.

## 핵심 구현

### 1. 데이터 중심의 포켓몬·기술 설계

`Poketmon` 클래스는 기본 능력치, 현재 HP, 랭크, 상태 이상, 레벨, 경험치와 보유 기술을 관리합니다. `Skill` 클래스는 공격에 필요한 값뿐 아니라 부가 효과까지 하나의 데이터 모델로 표현합니다. 새로운 포켓몬과 기술을 목록에 추가해 기존 전투 로직에서 재사용할 수 있도록 구성했습니다.

### 2. 단계별 데미지 계산

```text
공격/특수공격 × 기술 위력 ÷ 방어/특수방어
        × 속성 상성 × 자속 보정 × 급소 보정
```

물리·특수 기술은 서로 다른 능력치를 참조하며, 현재 HP 비례 기술은 별도의 계산 경로로 처리합니다. 이후 HP를 갱신하고 상성 및 급소 결과를 전투 메시지로 출력합니다.

### 3. 확률과 상태를 포함한 턴 처리

한 턴 안에서 명중 판정, 상태 이상에 따른 행동 가능 여부, 기술의 부가 효과, 랭크 변화, 회복, 전투 불능 여부를 순서대로 처리합니다. 플레이어와 상대의 스피드를 비교해 선공을 정하고, 교체와 아이템 사용도 하나의 행동으로 반영합니다.

### 4. 좌표 기반 콘솔 렌더링

일반적인 순차 출력 대신 화면 영역별 좌표를 정해 필요한 정보만 다시 그립니다. HP와 경험치는 현재 값에 비례한 막대로 표현하고, 상태 이상과 선택 메뉴는 색상을 달리해 텍스트 환경에서도 전투 상황을 빠르게 파악할 수 있도록 했습니다.

## 프로젝트 구조

```text
Poketmon2/
├─ main.cpp          # 게임 루프, 입력, 전투 처리, 콘솔 UI
├─ Poketmon.h/.cpp   # 포켓몬 상태와 성장 로직
├─ Skill.h/.cpp      # 기술 데이터 모델
├─ List_pok.h        # 포켓몬 및 파티 데이터
├─ List_skill.h      # 기술 데이터
├─ Type.h            # 타입 정의
├─ Item.h            # 아이템 정의
└─ TItle.h/.cpp      # 타이틀 및 진화 연출
```

## 조작 방법

- 숫자 키 `1`~`4`: 메뉴 또는 기술 선택
- `Enter`: 선택 확정
- `Backspace`: 이전 메뉴

## 실행 방법

1. Windows에서 `Poketmon2.sln`을 Visual Studio 2022로 엽니다.
2. 빌드 구성을 `Debug` 또는 `Release`, 플랫폼을 `x86` 또는 `x64`로 선택합니다.
3. 솔루션을 빌드한 뒤 실행합니다.

Windows Console API와 `_getch()`를 사용하므로 Windows 환경에서 실행해야 합니다.

## 배운 점

- 클래스와 객체를 활용해 포켓몬과 기술의 책임을 분리하고 게임 데이터를 모델링했습니다.
- 복수의 조건과 확률이 얽힌 턴제 전투 규칙을 구현하며 상태 관리와 처리 순서의 중요성을 익혔습니다.
- 콘솔 좌표, 색상, 입력을 직접 제어하며 제한된 환경에서도 사용자에게 명확한 피드백을 전달하는 방법을 고민했습니다.
- 포켓몬과 기술을 데이터로 분리해 콘텐츠 확장성과 공통 전투 로직의 재사용성을 높였습니다.

## 개선 방향

- 플레이어와 상대에게 중복된 턴 처리 코드를 공통 전투 함수로 분리
- 열거형과 상수 테이블을 활용해 상태 이상 및 능력치 효과의 가독성과 타입 안정성 개선
- 난수 생성기를 게임 시작 시 한 번만 초기화하고 테스트 가능한 전투 로직으로 분리
- 스마트 포인터와 표준 컨테이너를 적용해 객체 수명과 소유권을 명확하게 관리
- 전투 규칙에 대한 단위 테스트 추가

## Resume Summary

> C++와 Windows Console API를 활용해 포켓몬 스타일의 턴제 배틀 게임을 개발했습니다. 포켓몬·기술을 클래스로 모델링하고, 속성 상성, 물리/특수 데미지, 명중 및 급소 판정, 6종의 상태 이상, 능력치 랭크, 파티 교체, 아이템, 경험치와 진화 시스템을 구현했습니다. 또한 콘솔 좌표와 색상을 직접 제어해 HP/EXP 바와 전투 애니메이션을 표현했습니다.

---

이 프로젝트는 학습 목적으로 제작된 비공식 팬 프로젝트이며, 원작 및 관련 상표의 권리는 각 권리자에게 있습니다.
